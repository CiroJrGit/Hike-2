import React from 'react';
import { Container } from './styles';

const Button: React.FC = () => {
    return (
        <Container>
            <span>Postar</span>
        </Container>
    );
}

export default Button;